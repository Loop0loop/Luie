export const SEARCH_CONTEXT_RADIUS = 50;
// NOTE: Notion/Obsidian과 같은 즉시 저장 체감. .luie export는 전체 패키지 재직소라
// 매 키스트로크 실행은 과하므로 타이핑 정지 후 짧게만 지연시켜 coalescing한다.
export const PACKAGE_EXPORT_DEBOUNCE_MS = 500;
export const EDITOR_AUTOSAVE_DEBOUNCE_MS = 300;
export const DEFAULT_BUFFERED_INPUT_DEBOUNCE_MS = 250;
