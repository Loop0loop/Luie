/**
 * @deprecated 이 파일은 하위 호환성을 위한 re-export shim입니다.
 * 실제 구현은 ../utils/canvasProjectionAdapter.ts에 있습니다.
 * 새 코드는 ../utils/canvasProjectionAdapter를 직접 import하세요.
 */
export { buildProjection } from "../utils/canvasProjectionAdapter";
