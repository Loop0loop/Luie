/**
 * Public API for the entity visualization subfeature.
 *
 * Consumers should import from `.../wiki/visual` only — internal modules
 * (constants, mock data, hooks, sub-components) are implementation details.
 */

export { EntityVisualPanel } from "./EntityVisualPanel";
export type { EntityKind } from "./types";
