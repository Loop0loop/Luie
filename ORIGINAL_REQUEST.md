# Original User Request

## Initial Request — 2026-06-11T00:12:11+09:00

AnalysisSection 하단 및 레이아웃을 수정하여 불필요한 6개 검토 패널을 제거하고, fixView와 floatingView 두 가지 뷰 모드를 지원하는 SPA 인터페이스를 구현한다.

Working directory: /Users/user/Luie
Integrity mode: development

## Requirements

### R1. 불필요한 검토 패널 제거
- `충돌 큐`, `검토할 에피소드`, `검토할 사실`, `검토할 엔티티`, `검토할 별칭`, `메모리 평가` 패널 및 관련 API 연동 코드(hooks)를 제거한다.
- `서사 요약` 패널은 기존대로 유지한다.

### R2. 2가지 뷰 모드 지원 (SPA)
- **fixView**: 기존처럼 사이드바/패널 영역에 내장되어 온전히 대화창과 서사 요약 패널을 제공하는 모드.
- **floatingView**: 사이드바에는 최소화된 대화 아이콘(또는 활성화 버튼)만 남기고, 화면 중앙/최상위 레이어에 둥둥 뜨는 팝업식 미니 대화창(Mini Chat Window)을 렌더링하는 모드.
- 뷰 모드 상태는 전역 스토어(`analysisStore`) 혹은 기존 UI스토어로 관리되어 탭 전환 시에도 유지되어야 한다.
- 미니 대화창은 React Portal을 사용하여 최상위에 마운트한다.
- **드래그 기능:** 미니 대화창의 헤더 영역을 마우스로 잡고 화면 내에서 자유롭게 위치를 이동(드래그)할 수 있어야 한다.
- **전환 버튼 배치:** 고정 뷰 패널 상단(기존 타이틀 우측) 및 미니 대화창 헤더 영역에 각각 뷰 전환 아이콘 버튼(예: `ExternalLink` 혹은 `Expand` 관련 Lucide 아이콘)을 배치한다.

### R3. 리퀴드 스타일 UI 적용
- 래퍼런스(Colab, ChatGPT APP, Gemini APP) 스타일의 둥근 모서리, 블러 배경 효과 및 애니메이션 적용.

## Acceptance Criteria

### A1. 패널 삭제
- [ ] AnalysisSection에서 6개 패널과 큐 조회 API 요청 훅 호출이 완전히 제거되었는가?

### A2. 뷰 모드 전환 및 SPA 상태 보존
- [ ] 패널 상단 및 미니 대화창 헤더에 각각 뷰 전환 버튼이 제공되는가?
- [ ] floatingView 상태에서 다른 탭으로 이동했다가 복귀해도 floating 윈도우가 정상 유지되는가?
- [ ] Portal을 사용하여 화면 상의 최상위 레이어(top layer)에서 미니 대화창이 팝업으로 렌더링되는가?
- [ ] 미니 대화창을 헤더 드래그를 통해 화면 내에서 이동시킬 수 있는가?
- [ ] 미니 대화창 내에서 Q&A 기능이 정상 동작하는가?

## Follow-up — 2026-06-11T01:06:21+09:00

사용자 피드백을 전달합니다. 아래 사항들을 바탕으로 UI를 대대적으로 개선하고 고도화해 주세요.

1. **유리 질감(Glassmorphism) 극대화:**
   - fixView와 floatingView 모두 무색의 플랫한 느낌을 지우고, 세련된 유리 질감을 적용해 주세요.
   - `backdrop-blur-lg` 또는 `backdrop-blur-xl`을 적용하고, 배경색 투명도를 미세하게(`bg-panel/40` 또는 `bg-surface/30` 등) 얇게 주어 비쳐 보이게 하세요.
   - 은은한 반투명 테두리(`border-white/10` 또는 적절한 투명 보더)와 입체적인 shadow(`shadow-modal`, `shadow-panel`)를 결합해 고급스러운 유리 레이어를 완성해 주세요.

2. **가로로 긴 리퀴드 스타일 입력창:**
   - 캡슐 형태의 입력창이 가로로 충분히 길게 늘어날 수 있도록 레이아웃 너비 조정을 리퀴드(Liquid) 스타일에 맞춰 가로형 캡슐로 개선해 주세요.

3. **Floating Icon(최소화 버블) 및 전역 영속화:**
   - floatingView 모드일 때 대화창을 닫거나 최소화하면 완전히 닫히는 대신, 화면 우하단 등에 세련된 둥근 플로팅 챗 아이콘(FAB)으로 최소화되게 해 주세요.
   - 이 플로팅 아이콘을 누르면 리퀴드 애니메이션으로 미니 대화창이 팝업됩니다.
   - 다른 탭으로 이동해도 이 플로팅 아이콘/윈도우가 화면 상에 영속적으로 둥둥 떠 있도록 Portal 및 Store 상태를 정교화해 주세요.

4. **크기 조절(Resizable) 기능:**
   - 사용자가 미니 대화창의 크기를 원하는 대로 늘리고 줄일 수 있도록 크기 조절 기능을 제공해 주세요. 
   - 우하단에 마우스 리사이즈용 핸들을 구현하거나 CSS `resize` 기능을 결합하여 너비와 높이를 조절할 수 있게 하되, 최소 크기(예: 320x400)와 최대 크기(예: 600x800)의 바운더리를 제한해 주세요.

