# Production Environment Variables
# 이 파일은 패키지된 앱 실행 시 사용됩니다
# userData 경로는 런타임에 동적으로 설정됩니다

# Database (앱 데이터 디렉토리에 자동 생성)
# DATABASE_URL은 코드에서 동적으로 설정됨

# Logging
LOG_LEVEL=info

# Security
NODE_ENV=production
